

export PATH=/usr/local/cuda-12.8/bin:$PATH
export LD_LIBRARY_PATH=/usr/local/cuda-12.8/lib64:$LD_LIBRARY_PATH

rm -rf build && mkdir build && cd build

cmake ..

make -j$(nproc)


ldd ./OptiXDenoiser.so

